[project]
name = "project-afia"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rexford27", email = "rexfordfrempong7@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[project.scripts]
project-afia = "project_afia:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
